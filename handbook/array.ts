const arr: Person[] = []; // can use this to identify the returned data interface from the database for eg

// Tuples
type MyList = [number?, string?, boolean?];

const arr1: MyList = [];

arr1.push(1, 2);
arr1.push('23');
arr1.push(true);
